tasks:
  Init Google OAuth:
    robotTaskName: Init Google OAuth
  Init Microsoft OAuth:
    robotTaskName: Init Microsoft OAuth
  Send Google Email:
    robotTaskName: Send Google Email
  Send Microsoft Email:
    robotTaskName: Send Microsoft Email

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
