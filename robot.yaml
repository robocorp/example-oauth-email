tasks:
  Init OAuth Flow:
    robotTaskName: Init OAuth Flow
  Send Google Email:
    robotTaskName: Send Google Email
  Send Microsoft Email:
    robotTaskName: Send Microsoft Email

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
